spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      profile: dev
eureka:
  instance:
#    instance-id: product1
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
