# 这个文件以bootstrap为名,表示启动时这个配置文件的优先级很高,解决的问题在于:
# 如果把配置文件放在了远端的仓库(配置文件中包括数据库配置等等信息) 我们希望先去把配置文件拉下来
# 再去拿数据库的配置文件去连接,但是文件名用application.yml的时候,他会搞不清顺序,所以就找不到
# 数据库的配置而报错,这个就是确保先去拿到配置再去加载别的东西


#注意 config-client会默认去localhost:8888 找配置,所以我把serverport配置成8888
# application name还是要配置一下的
spring:
  application:
    name: order
  datasource:
    username: root
    password: w4247794
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://120.27.231.86/cloud_order?characterEncoding=utf-8&useSSL=false
  jpa:
    show-sql: true
#  cloud:
#    config:
#      label: master
#      uri: http://localhost:8888/
#      discovery:
#        enabled: true
#        service-id: CONFIG-SERVER
#      profile: dev
server:
  port: 8600
eureka:
  instance:
#    instance-id: order1
    lease-expiration-duration-in-seconds: 90
    lease-renewal-interval-in-seconds: 20
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8000/eureka/